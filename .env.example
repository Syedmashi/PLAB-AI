# Server-side only. Used by api/gemini.ts.
# Do not expose this through Vite client variables or commit real keys.
GEMINI_API_KEY="your_server_side_gemini_api_key"

# Optional canonical app URL for future callbacks/links.
APP_URL="http://localhost:3000"
